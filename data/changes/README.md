# Change Storage

This directory stores rule-based change detection outputs.

Subdirectories:

- `change_logs/`: detected new events, page changes, and jobs changes
- `detection_runs/`: batch-level summaries of change detection

Each file is grouped by batch date, for example `2026-04-13.jsonl`.
