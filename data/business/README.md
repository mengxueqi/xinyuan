# Business Database

This directory stores the local business database used by the application layer.

Current implementation:

- SQLite database file: `xinyuan.db`

Purpose:

- power dashboard queries
- support notification state tracking
- support daily/weekly report generation
- provide a stable product-facing data model separate from raw files
