# Processed Storage

This directory stores rule-based processing outputs derived from `data/raw`.

Subdirectories:

- `clean_documents/`: normalized and annotated raw documents
- `event_candidates/`: structured event candidates for later review
- `processing_runs/`: batch-level processing summaries

Each file is grouped by raw batch date, for example `2026-04-13.jsonl`.
