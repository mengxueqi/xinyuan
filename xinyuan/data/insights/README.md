# Insight Storage

This directory stores rule-based insight outputs derived from `data/changes`.

Subdirectories:

- `insight_items/`: scored and summarized insight records
- `insight_runs/`: batch-level summaries of insight generation

Each file is grouped by batch date, for example `2026-04-13.jsonl`.
